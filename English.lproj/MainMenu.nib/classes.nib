{
    IBClasses = (
        {
            ACTIONS = {
                addFiles = id; 
                insertPlaylist = id; 
                insertPlaylistWithSelection = id; 
                insertSmartPlaylist = id; 
                insertWatchFolder = id; 
                jumpToLibrary = id; 
                jumpToNowPlaying = id; 
                jumpToPlayQueue = id; 
                nextPlaylist = id; 
                playNextStream = id; 
                playPause = id; 
                playPreviousStream = id; 
                previousPlaylist = id; 
                removeSelectedStreams = id; 
                showPlaylistInformationSheet = id; 
                showStreamInformationSheet = id; 
                skipBackward = id; 
                skipForward = id; 
                skipToBeginning = id; 
                skipToEnd = id; 
                toggleBrowser = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = PlayApplicationDelegate; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {checkForUpdates = id; }; 
            CLASS = SUUpdater; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}